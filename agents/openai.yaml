interface:
  display_name: "Skill Builder"
  short_description: "Build portable project-specific agent skills"
  default_prompt: "Use $skill-builder to design a portable project skill system."
policy:
  allow_implicit_invocation: false
