# Runtime Adapter Template

Copy this file to `.agents/adapters/{runtime}/README.md` only after verifying the runtime integration.

```markdown
# {Runtime} adapter

Canonical source: `../../skills/{project}-{capability}/SKILL.md`

Installation: {verified command or manual action}

Invocation/discovery: {verified behavior, or "not automatic"}

Known differences: {none, or exact translation/limitation}
```

Do not duplicate or alter the canonical procedure here.
