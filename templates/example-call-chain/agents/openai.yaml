interface:
  display_name: "L1-functional — example-call-chain"
  short_description: "L1-functional — Call-chain tracing skill template. API data flow, type matching, endpoint verification."
  default_prompt: "Use example-call-chain to trace API call chains and verify data flow correctness."
policy:
  allow_implicit_invocation: false
triggers:
  - 调用链
  - 数据流
  - 类型匹配
  - 链路追踪
  - API 追踪
  - 参数传递
